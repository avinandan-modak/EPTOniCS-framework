# Scripts

Reserved for repository-level automation that spans more than one
example (e.g. batch HPC job submission templates, result aggregation,
figure regeneration for the manuscript). Empty in this release;
per-example local job launchers currently live beside each driver as
`examples/<name>/run_local.sh`, since each launcher's relative paths
and output directory are specific to that one example.
